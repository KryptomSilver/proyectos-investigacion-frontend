import React, { Fragment } from "react";
import Nav from "../components/Nav";

const Home = () => {
    return (
        <Fragment>
            <Nav />
        </Fragment>
    );
};

export default Home;
